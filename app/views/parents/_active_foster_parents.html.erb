<div class="card">
        <div class="card-content">
            <h4>Active Foster Parents</h4>

            <% if @active_parents.empty? %>
                <p>There are no active foster parents on file at this time.</p>
             
            <%else %>

                <table class = "striped highlight">
                    <thead>
                        <th>Foster Parent Names</th>
                        <th>Email</th>
                        <th>Assigned Forms</th>
                        <th>Incomplete</th>
                        
                    </thead>
                    <% for parent in @active_parents %>
                        <% if parent.active?%>
                            <tr>
                                <td><%= link_to "#{parent.p1_last_name}, #{parent.p1_first_name}", parent_path(parent)%></td>
                                <td><%= "#{parent.email}" %> </td>
                                <td><%= "3 Forms or whatevs"%></td>
                                <td><%= "3 Incomplete or whatevs"%></td>
                            </tr>
                        <% end %>
                    <% end %>
                </table>
            <%end%>
        </div>
    </div>