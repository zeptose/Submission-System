<% flash.each do |name, msg| %>
  <div id="flash_box" data-alert data-options="animation_speed:500, animation:'fadeOut';" class="alert-box <%= flash[:notice] ? "success" : "warning" %>">
    <!-- <a href="#" class="close">&times;</a> -->
      <%= msg %>
  </div>
<% end %>